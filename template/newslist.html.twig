{% extends 'layout.html.twig' %}
{% from 'shared.html.twig' import render_news %}

{% block content %}

<h2>{{ title }}</h2>

<newslist>
    {% for news in newslist %}
        {{ render_news(news) }}
    {% endfor %}
</newslist>

{% endblock %}
